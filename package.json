{
  "name": "design-tokens",
  "version": "1.0.0",
  "main": "./dist/parser.js",
  "scripts": {
    "test": "tap test.ts --no-coverage --reporter specy --ts",
    "build": "esbuild lib/parser.ts --outdir=dist --format=cjs --platform=node --target=es2016",
    "fix": "standard test.ts --fix && ts-standard ./parser.ts --fix"
  },
  "keywords": ["design-system", "token"],
  "author": "hawyar",
  "license": "MIT",
  "devDependencies": {
    "@types/tap": "^15.0.7",
    "@types/tinycolor2": "^1.4.3",
    "esbuild": "^0.14.49",
    "tap": "^16.3.0",
    "ts-node": "^10.9.1"
  },
  "dependencies": {
    "no-case": "^3.0.4",
    "tinycolor2": "^1.4.2"
  }
}
