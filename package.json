{
  "name": "design-tokens",
  "version": "1.0.0",
  "scripts": {
    "dev": "tsx parser.ts",
    "test": "rm -rf ./dist && npm run build && tap ./test.js --no-coverage --reporter specy",
    "build": "esbuild parser.ts --outdir=dist --format=cjs --platform=node --target=es2016"
  },
  "keywords": [],
  "author": "hawyar",
  "license": "MIT",
  "devDependencies": {
    "@types/tap": "^15.0.7",
    "esbuild": "^0.14.49",
    "tap": "^16.3.0"
  }
}
